import React from "react";

import { PropsBug } from "./PropsBug";

export default {
  title: "Example/PropsBug",
  component: PropsBug,
};

const Template = (args) => <PropsBug />;

export const Primary = Template.bind({});
Primary.args = {};
